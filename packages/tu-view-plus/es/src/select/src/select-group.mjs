import { buildProps as o } from "@tu-view-plus/utils";
const p = o({
  /**
   * @zh 选项组的标题
   * @en Title of option group
   */
  label: {
    type: String
  }
});
export {
  p as selectGroupProps
};
