import { buildProps as o } from "@tu-view-plus/utils";
const e = o({
  activeTabRef: {
    type: Object
  },
  direction: {
    type: String
  },
  disabled: Boolean,
  animation: Boolean
});
export {
  e as tabsNavIndicateProps
};
