import { withInstall } from '@tu-view-plus/utils';
import CollapseTransition from './src/collapse-transition.vue';

export const TuCollapseTransition = withInstall(CollapseTransition);

export default TuCollapseTransition;
