import { withInstall } from '@tu-view-plus/utils';
import ResizeObserver from './src/resize-observer.vue';

export const TuResizeObserver = withInstall(ResizeObserver);
export default TuResizeObserver;
