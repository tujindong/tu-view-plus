import { withInstall } from '@tu-view-plus/utils'
import Scrollbar from './src/scrollbar.vue'

export const TuScrollbar = withInstall(Scrollbar)
export default TuScrollbar

export * from './src/scrollbar'
